import React from "react";

const Loading = () => {
  return (
    <section className="section">
      <div className="loading">
        <h1>loading...</h1>
      </div>
    </section>
  );
};

export default Loading;
